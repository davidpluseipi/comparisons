#include<stdio.h>
#include<stdlib.h>

int main()
{
    // numbers
    // integer
    int pin = 4004;

    // decimal
    double gpa = 3.7;
    float gpa2 = 4.0;

    // characters
    char grade = 'A'; // char can only hold one character
    // strings (or really a 'string' of characters)
    char phrase[] = "The students have high GPAs."; // the [] creates an array. Char arrays can't be modified later.
    
    


    return 0;
}